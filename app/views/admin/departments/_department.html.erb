
<p>
	<tr>
	<td><%= department.title %></td>
</p>	
	<td><%= link_to 'Show', admin_department_path(department.id)%></td>
	<td><%= link_to 'Edit', edit_admin_department_path(department.id) %></td>
	<td><%= link_to 'Destroy', admin_department_path(department.id), :confirm => 'Are you sure?', :method => :delete %></td>